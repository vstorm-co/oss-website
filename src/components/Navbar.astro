---
import ThemeToggle from './ThemeToggle.astro';
import LanguageSwitcher from './LanguageSwitcher.astro';
import { translations, type Lang } from '../i18n/translations';
import type { Project } from '../data/projects';

interface Props {
  currentLang?: Lang;
  showLang?: boolean;
  project?: Project | null;
  pathSuffix?: string;
}

const { currentLang = "en", showLang = true, project, pathSuffix } = Astro.props;
const t = translations[currentLang];
const rawBase = import.meta.env.BASE_URL;
const base = rawBase.endsWith("/") ? rawBase : rawBase + "/";

const homeHref = currentLang === "en" ? "/" : `/${currentLang}/`;
const blogHref = currentLang === "en" ? "/blog/" : `/${currentLang}/blog/`;
const aboutHref = currentLang === "en" ? "/about/" : `/${currentLang}/about/`;
const githubHref = project?.github
  ? `https://github.com/${project.github}`
  : "https://github.com/vstorm-co";
const siteName = project ? project.name : "Vstorm OSS";
---

<nav class="fixed top-0 left-0 right-0 z-40 border-b border-border/50 bg-bg/80 backdrop-blur-lg">
  <div class="mx-auto max-w-7xl flex items-center justify-between px-4 sm:px-6 h-14">
    <!-- Left: Logo -->
    <div class="flex flex-1 items-center gap-1.5 min-w-0">
      <a href={homeHref} class="flex items-center gap-2 text-text hover:text-accent transition-colors shrink-0">
        <img src={`${base}favicon.png`} alt="Vstorm OSS" class="w-6 h-6" width="24" height="24" />
        <span class="text-sm font-semibold tracking-tight hidden sm:inline">Vstorm OSS</span>
      </a>
      {project && (
        <>
          <span class="text-text-tertiary hidden sm:inline">/</span>
          <span class="text-sm font-medium text-text-secondary hidden sm:inline truncate max-w-[200px]">{project.name}</span>
        </>
      )}
    </div>

    <!-- Center: Links -->
    <div class="flex items-center gap-1">
      <a
        href={homeHref}
        class="flex items-center gap-1.5 px-3 py-1.5 text-sm text-text-secondary hover:text-text transition-colors rounded-lg hover:bg-surface-hover"
      >
        <!-- Grid icon -->
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 0 1 6 3.75h2.25A2.25 2.25 0 0 1 10.5 6v2.25a2.25 2.25 0 0 1-2.25 2.25H6a2.25 2.25 0 0 1-2.25-2.25V6ZM3.75 15.75A2.25 2.25 0 0 1 6 13.5h2.25a2.25 2.25 0 0 1 2.25 2.25V18a2.25 2.25 0 0 1-2.25 2.25H6A2.25 2.25 0 0 1 3.75 18v-2.25ZM13.5 6a2.25 2.25 0 0 1 2.25-2.25H18A2.25 2.25 0 0 1 20.25 6v2.25A2.25 2.25 0 0 1 18 10.5h-2.25a2.25 2.25 0 0 1-2.25-2.25V6ZM13.5 15.75a2.25 2.25 0 0 1 2.25-2.25H18a2.25 2.25 0 0 1 2.25 2.25V18A2.25 2.25 0 0 1 18 20.25h-2.25a2.25 2.25 0 0 1-2.25-2.25v-2.25Z"/>
        </svg>
        <span class="hidden sm:inline">{t["nav.projects"]}</span>
      </a>
      <a
        href={blogHref}
        class="flex items-center gap-1.5 px-3 py-1.5 text-sm text-text-secondary hover:text-text transition-colors rounded-lg hover:bg-surface-hover"
      >
        <!-- Blog icon -->
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M19.5 14.25v-2.625a3.375 3.375 0 0 0-3.375-3.375h-1.5A1.125 1.125 0 0 1 13.5 7.125v-1.5a3.375 3.375 0 0 0-3.375-3.375H8.25m0 12.75h7.5m-7.5 3H12M10.5 2.25H5.625c-.621 0-1.125.504-1.125 1.125v17.25c0 .621.504 1.125 1.125 1.125h12.75c.621 0 1.125-.504 1.125-1.125V11.25a9 9 0 0 0-9-9Z"/>
        </svg>
        {t["nav.blog"]}
      </a>
      <a
        href={aboutHref}
        class="flex items-center gap-1.5 px-3 py-1.5 text-sm text-text-secondary hover:text-text transition-colors rounded-lg hover:bg-surface-hover"
      >
        <!-- About icon -->
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M15 19.128a9.38 9.38 0 0 0 2.625.372 9.337 9.337 0 0 0 4.121-.952 4.125 4.125 0 0 0-7.533-2.493M15 19.128v-.003c0-1.113-.285-2.16-.786-3.07M15 19.128v.106A12.318 12.318 0 0 1 8.624 21c-2.331 0-4.512-.645-6.374-1.766l-.001-.109a6.375 6.375 0 0 1 11.964-3.07M12 6.375a3.375 3.375 0 1 1-6.75 0 3.375 3.375 0 0 1 6.75 0Zm8.25 2.25a2.625 2.625 0 1 1-5.25 0 2.625 2.625 0 0 1 5.25 0Z"/>
        </svg>
        {t["nav.about"]}
      </a>
    </div>

    <!-- Right: Language + Theme -->
    <div class="flex flex-1 items-center justify-end gap-2">
      {showLang && <LanguageSwitcher currentLang={currentLang} pathSuffix={pathSuffix} />}
      <ThemeToggle />
    </div>
  </div>
</nav>

<!-- Spacer so content isn't hidden behind fixed navbar -->
<div class="h-14"></div>
