---
import type { translations } from '../../i18n/translations';
import StarsBadge from '../shared/StarsBadge.astro';

interface Props {
  t: typeof translations.en;
  basePath?: string;
}

const { t, basePath = "/" } = Astro.props;

const terminalLines = [
  { prefix: "$", text: "pip install fastapi-fullstack", color: "text-text" },
  { prefix: "$", text: "fastapi-fullstack new", color: "text-text" },
  { prefix: "?", text: 'Project name: <span class="text-text">my_app</span>', color: "text-text-secondary" },
  { prefix: "?", text: 'AI framework: <span class="text-text">Pydantic AI</span>', color: "text-text-secondary" },
  { prefix: "?", text: 'Database: <span class="text-text">PostgreSQL</span>', color: "text-text-secondary" },
  { prefix: "✓", text: t["hero.terminal.success"], color: "text-text", prefixColor: "text-accent" },
];
---

<section class="relative overflow-hidden px-4 pt-32 pb-24 sm:px-6 sm:pt-40 sm:pb-28 lg:px-8 lg:pt-48 lg:pb-36">
  <!-- Subtle gradient glow -->
  <div class="pointer-events-none absolute inset-0">
    <div class="absolute top-[5%] left-1/2 h-[400px] w-[500px] sm:h-[800px] sm:w-[1000px] -translate-x-1/2 rounded-full bg-accent/[0.08] blur-[120px] sm:blur-[200px]"></div>
    <div class="absolute top-[10%] left-[40%] h-[300px] w-[350px] sm:h-[600px] sm:w-[700px] -translate-x-1/2 rounded-full bg-violet-500/[0.04] blur-[100px] sm:blur-[160px]"></div>
  </div>
  <!-- Bottom fade-out gradient -->
  <div class="pointer-events-none absolute inset-x-0 bottom-0 h-32 bg-gradient-to-t from-bg to-transparent"></div>

  <div class="relative mx-auto max-w-4xl text-center">
    <!-- Badge -->
    <div class="mb-8 inline-flex items-center rounded-full border border-border px-4 py-1.5 text-sm text-text-secondary">
      <span class="mr-2 inline-block h-1.5 w-1.5 rounded-full bg-accent"></span>
      {t["hero.badge"]}
    </div>

    <!-- Headline -->
    <h1 class="text-4xl font-bold tracking-[-0.02em] sm:text-5xl lg:text-6xl">
      {t["hero.title.line1"]}
      <span class="bg-gradient-to-r from-accent to-violet-500 bg-clip-text text-transparent">{t["hero.title.highlight"]}</span>
      <br />
      {t["hero.title.line2"]}
    </h1>

    <!-- Description -->
    <p class="mx-auto mt-6 max-w-2xl text-lg leading-relaxed text-text-secondary">
      {t["hero.description"]}
    </p>

    <!-- CTA Buttons -->
    <div class="mt-10 flex flex-col items-center justify-center gap-4 sm:flex-row">
      <a
        href={`${basePath}configurator/`}
        class="inline-flex items-center rounded-full bg-accent px-8 py-3 text-base font-semibold text-white transition-colors hover:bg-accent-hover"
      >
        {t["hero.cta.configure"]}
        <svg class="ml-2 h-4 w-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13 7l5 5m0 0l-5 5m5-5H6" />
        </svg>
      </a>
      <a
        href="https://github.com/vstorm-co/full-stack-ai-agent-template"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center rounded-full border border-border px-8 py-3 text-base font-semibold text-text transition-colors hover:border-border-hover hover:bg-surface"
      >
        {t["hero.cta.github"]}
        <StarsBadge repo="vstorm-co/full-stack-ai-agent-template" />
      </a>
    </div>

    <!-- Terminal Preview -->
    <div class="mx-auto mt-16 max-w-xl overflow-hidden rounded-2xl border border-border bg-surface shadow-2xl shadow-black/20">
      <div class="flex items-center gap-2 border-b border-border px-4 py-3">
        <span class="h-2.5 w-2.5 rounded-full bg-red-500/60"></span>
        <span class="h-2.5 w-2.5 rounded-full bg-yellow-500/60"></span>
        <span class="h-2.5 w-2.5 rounded-full bg-green-500/60"></span>
        <span class="ml-2 text-xs text-text-tertiary">{t["ui.terminal"]}</span>
      </div>
      <div id="terminal" class="px-5 py-5 text-left font-mono text-sm leading-loose min-h-[210px]">
        {/* Lines rendered by JS typing animation */}
      </div>
    </div>
  </div>

  <!-- Scroll arrow -->
  <button class="hero-scroll-arrow" aria-label="Scroll down" onclick="this.closest('section').nextElementSibling?.scrollIntoView({behavior:'smooth'})">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M7 10l5 5 5-5"/></svg>
  </button>
</section>

<script is:inline define:vars={{ terminalLines }}>
  document.addEventListener('astro:page-load', function() {
    var container = document.getElementById('terminal');
    if (!container || container.dataset.typed) return;
    container.dataset.typed = '1';

    var charDelay = 28;
    var lineDelay = 400;
    var lines = terminalLines;
    var lineIndex = 0;

    function typeLine() {
      if (lineIndex >= lines.length) return;

      var line = lines[lineIndex];
      var p = document.createElement('p');
      var prefixSpan = document.createElement('span');
      prefixSpan.className = line.prefixColor || 'text-text-tertiary';
      prefixSpan.textContent = line.prefix;
      p.appendChild(prefixSpan);
      p.appendChild(document.createTextNode(' '));

      var textSpan = document.createElement('span');
      textSpan.className = line.color;
      p.appendChild(textSpan);

      // Cursor
      var cursor = document.createElement('span');
      cursor.className = 'typing-cursor';
      p.appendChild(cursor);

      container.appendChild(p);

      // For lines with inner HTML (like "Project name: <span>my_app</span>"),
      // type the plain text version then set innerHTML at the end
      var fullHtml = line.text;
      var tmp = document.createElement('div');
      tmp.innerHTML = fullHtml;
      var plainText = tmp.textContent || '';

      var charIndex = 0;
      function typeChar() {
        if (charIndex < plainText.length) {
          textSpan.textContent += plainText[charIndex];
          charIndex++;
          setTimeout(typeChar, charDelay);
        } else {
          // Done typing — set rich HTML and remove cursor
          textSpan.innerHTML = fullHtml;
          cursor.remove();
          lineIndex++;
          setTimeout(typeLine, lineDelay);
        }
      }
      typeChar();
    }

    // Start after a small delay so user sees the empty terminal first
    setTimeout(typeLine, 600);
  });
</script>
